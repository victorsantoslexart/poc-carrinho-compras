import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  iconButton: {
    color: '#F92E6A',
    fontSize: 20,
  },
});

export default styles;
